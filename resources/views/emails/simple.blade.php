<!DOCTYPE html>
<html>
    <meta charset="utf-8">
    <title>{{ $data['subject'] }}</title>
    <body>
        <h1>{{ $data['subject'] }}</h1>
        <p>{{ $data['message'] }}</p>
    </body>
</html>